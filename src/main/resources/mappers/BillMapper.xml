<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace是命名空间，是mapper接口的全路径 -->
<mapper namespace="cn.reimbursement.dao.BillDao">

	<!--resultMap – 是最复杂也是最强大的元素，用来描述如何从数据库结果集中来加载对象 -->
	<resultMap id="billMap"
		type="cn.reimbursement.pojo.Bill">
		<id property="billId" column="bill_id" />
		<result property="billAttribute" column="bill_attribute" />
		<result property="billCompany"
			column="bill_company" />
		<result property="billType" column="bill_type" />
		<result property="billReimbursementDep" column="bill_reimbursement_dep" />
		<result property="billSubject" column="bill_subject" />
		<result property="billBelongCompany" column="bill_belong_company" />
		<result property="billReimbursementPerson" column="bill_reimbursement_person" />
		<result property="billSummary" column="bill_summary" />
		<result property="billAmount" column="bill_amount" />
		<result property="billInvoiceStatusName" column="bill_contract_status_name" />
		<result property="billInvoiceAmount" column="bill_invoice_amount" />
		<result property="billReimbursementPersonConfirm" column="bill_reimbursement_person_confirm" />
		<result property="billChargePerson" column="bill_charge_person" />
		<result property="billRegistrantPerson" column="bill_registrant_person" />
		<result property="billRegistrantDate" column="bill_registrant_date" />
		<result property="billIsEnd" column="bill_is_end" />
		<result property="billEndDate" column="bill_end_date" />
	</resultMap>

	<!--sql – 可被其他语句引用的可重用语句块 -->
	<sql id="billColums">
		bill_id,bill_attribute,bill_company,bill_type,bill_reimbursement_dep,bill_subject,bill_belong_company,bill_reimbursement_person,bill_summary,bill_amount,bill_contract_status_name,bill_invoice_amount,bill_reimbursement_person_confirm,
		bill_charge_person,bill_registrant_person,bill_registrant_date,bill_is_end,bill_end_date
	</sql>
	<select id="selectBillByCompany" resultMap="billMap">
		select <include refid="billColums"/> from bill where bill_company =#{billCompany} order by bill_registrant_date desc
	</select>
	<select id="selectBillById" resultType="int">
		select count(*) from bill where bill_id =#{billId}
	</select>
	<insert id="insertBill">
		insert into bill 
		<foreach collection="billMap.keys" separator="," item="key" open="(" close=")">
            ${key}
        </foreach>
        values
        <foreach collection="billMap.keys" item="key" separator="," open="(" close=")">
            #{billMap[${key}]}
        </foreach>
	</insert>

</mapper>